# Fully pinned (transitive closure) so the image always rebuilds identically.
# To upgrade: loosen, rebuild, then `pip freeze` back into this file.
# Direct deps: opencv-python-headless, dlib, flask, waitress, numpy
blinker==1.9.0
click==8.4.1
dlib==20.0.1
Flask==3.1.3
itsdangerous==2.2.0
Jinja2==3.1.6
MarkupSafe==3.0.3
numpy==2.5.0
opencv-python-headless==4.13.0.92
waitress==3.0.2
Werkzeug==3.1.8
